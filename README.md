# Concurrent_FTPServer
Industrial-grade Concurrent FTP Server implemented in C on Linux. Features multi-process concurrency via fork(), TCP/IP socket programming, and a custom chunk-based file transfer protocol.
